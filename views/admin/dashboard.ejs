<%- include('../partials/header', { title }) %>
<section class="admin-shell"><div class="container"><div class="admin-heading"><div><span class="eyebrow">KRL VERWALTUNG</span><h1>ADMIN-DASHBOARD</h1></div><form method="post" action="/admin/logout"><button class="button button-ghost" type="submit">Abmelden</button></form></div>
  <div class="admin-grid"><% modules.forEach(([key, config]) => { %><a class="admin-card" href="/admin/<%= key %>"><span><%= counts[key] %> Einträge</span><h2><%= config.title %></h2><small>Öffnen und bearbeiten →</small></a><% }) %></div>
</div></section>
<%- include('../partials/footer') %>
